<table class="table">
    <tbody>
        <thead>
            <tr>
                <th>First name</th>
                <th>Last name</th>
                <th>Average Rating</th>
                <th></th>
                <th></th>
                <th></th>
            </tr>
        </thead>

            <% items.each do |person| %>
        <tr>
                <td><%= person.first_name %></td>
                <td><%= person.last_name %></td>
                <td><%= person.average_rating %></td>
                <!-- Change this to a contact! button -->
        </tr>
            <% end %>
    </tbody>
</table>

