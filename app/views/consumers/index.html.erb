<h1>Listing consumers</h1>

<table>
  <thead>
    <tr>
      <th>Unique user</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @consumers.each do |consumer| %>
      <tr>
        <td><%= consumer.unique_user_id %></td>
        <td><%= link_to 'Show', consumer %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
