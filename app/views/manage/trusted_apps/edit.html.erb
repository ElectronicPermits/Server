<%= render partial: "shared/title", locals: {title: @trusted_app.app_name, 
                                             subtitle: "Modify Settings for #{@trusted_app.app_name} App"} %>

<%= render 'form' %>

<%= link_to 'Show', manage_trusted_app_path(@trusted_app) %> |
<%= link_to 'Back', manage_trusted_apps_path %>
